-- ============================================================
-- UG Campus Security & Emergency Response Optimizer
-- Seed Data
-- ============================================================

-- Canonical seed data lives in data/*.csv. CsvDatasetImporter validates and
-- imports all seven canonical datasets into SQLite, so this file intentionally
-- contains no duplicate INSERT statements. Benchmark measurements must come
-- from real algorithm runs and must never be fabricated as seed data.
